{
  "name": "camera-capture-mp",
  "version": "1.0.5",
  "type": "module",
  "description": "Generate poster using canvas through the configuration in miniprogram",
  "main": "app.js",
  "scripts": {
    "build": "node build.js"
  },
  "author": "limeng",
  "license": "MIT",
  "miniprogram": "dist/camera-capture-mp",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "eslint": "^8.44.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-import-resolver-alias": "^1.1.2",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-prettier": "^4.2.1",
    "lint-staged": "^13.2.3",
    "prettier": "^2.8.8",
    "stylelint": "^14.16.0",
    "stylelint-config-prettier": "^9.0.5",
    "stylelint-config-rational-order": "^0.1.2",
    "stylelint-config-standard": "^29.0.0",
    "stylelint-declaration-block-no-ignored-properties": "^2.7.0",
    "stylelint-order": "^6.0.3"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix --quiet",
      "prettier --write"
    ],
    "*.{wxss, css}": [
      "stylelint --fix",
      "prettier --write"
    ],
    "*.json": [
      "prettier --write"
    ],
    "*.md": [
      "prettier --write"
    ]
  }
}
